/**
 * Used enums
 */

export enum IconButtonType {
	None = 'none',
	Asset = 'asset',
	Default = 'default',
	Exercise = 'exercise',
	GitHub = 'github',
	Link = 'link',
	Pdf = 'pdf',
	YouTube = 'youtube',
}

export enum ReferenceType {
	Article = 'article',
	Podcast = 'podcast',
	Link = 'link',
	Video = 'video',
}
